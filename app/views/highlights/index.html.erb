<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>WatchGoals: View All Highlights</title>
  </head>
  <body>
    <div class="content-wrapper">
      <h1 id="index-header">All Highlights</h1>
      <% @highlights.each do |date, highlights| %>
        <div class="highlight-wrapper">
          <div class="highlight-date-wrapper">
          <h2><%= date.to_s %></h2>
          </div><!-- highlight-date-wrapper -->
          <% highlights.each do |highlight| %>
          <h3><%= highlight.title %></h3>
          <%= highlight.clean_media_embed %>
          <br>
          <a href=<%= "/highlights/#{highlight.id}" %>> View Details </a> |
          <a href=<%= highlight.url %> target="_blank">View Source Media</a> |
          <a href=<%= "http://www.reddit.com" + highlight.permalink %> target="_blank">View Reddit Post</a>
          <% end %>
        </div> <!-- highlight-wrapper -->
      <% end %>
    </div><!-- content-wrapper -->
  </body>
</html>
